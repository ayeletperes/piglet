% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{ascReconcile}
\alias{ascReconcile}
\title{Reconcile old and new ASC assignments with hysteresis}
\usage{
ascReconcile(old_assign, new_assign, changes, new_alleles)
}
\arguments{
\item{old_assign}{Named vector allele -> original ASC label (original alleles).}

\item{new_assign}{Named vector allele -> new cluster label (original + new).}

\item{changes}{A data.frame from \code{ascClassifyConfidence} (must have a
\code{confidence} column).}

\item{new_alleles}{Character vector of newly added allele names.}
}
\value{
A data.frame with columns \code{allele}, \code{old_asc},
\code{new_cluster}, \code{reconciled_asc}, \code{change_type},
\code{confidence}, \code{review_flag}.
}
\description{
Produces a reconciled allele -> ASC assignment that absorbs new alleles into
their best original ASC, creates brand-new ASCs, but only applies changes to
established ASCs (splits, merges, reassignments) where confidence is High.
Intermediate changes are retained as the old assignment and flagged for review.
}
