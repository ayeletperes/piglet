% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{.asc_fixed_similarity}
\alias{.asc_fixed_similarity}
\title{Fixed, dataset-independent distance-to-similarity transform}
\usage{
.asc_fixed_similarity(
  distance_matrix,
  method = c("linear", "log"),
  floor = 0.001
)
}
\arguments{
\item{distance_matrix}{A square distance matrix (or a \code{dist} object) with
values in [0, 1] (as returned by \code{igDistance(method = "decipher")}).}

\item{method}{\code{"linear"} (default) gives \code{S = 1 - d}; \code{"log"}
gives \code{S = min(-log(max(d, floor)), -log(floor)) / -log(floor)}.}

\item{floor}{Constant lower bound used by the \code{"log"} method (default
\code{1e-3}). It is a fixed constant, never derived from the data.}
}
\value{
A square similarity matrix with a zero diagonal and the same dimnames
as the input.
}
\description{
Converts a pairwise distance matrix to a similarity matrix without any
dataset-wide normalisation, so the similarity between two alleles depends only
on their pairwise distance and never changes when other alleles are added or
removed. This is the key property that lets ASCs stay stable as the reference
grows.
}
\keyword{internal}
