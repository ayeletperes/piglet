% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{ascRetention}
\alias{ascRetention}
\title{Per-ASC retention / continuity score}
\usage{
ascRetention(old_assign, new_assign, min_share = 0.1)
}
\arguments{
\item{old_assign}{Named vector mapping allele -> original ASC label.}

\item{new_assign}{Named vector mapping allele -> new cluster label (must cover
all original alleles plus any added ones).}

\item{min_share}{Minimum fraction of an original ASC that a new cluster must
receive to count as "spanned" (default 0.1).}
}
\value{
A data.frame with columns \code{old_asc}, \code{size},
\code{best_new_cluster}, \code{retention}, \code{n_new_spanned},
\code{n_moved}.
}
\description{
For each original ASC, measures how much of it stays together in the expanded
clustering: \code{retention(k) = max_j |k_old intersect j_new| / |k_old|}.
}
