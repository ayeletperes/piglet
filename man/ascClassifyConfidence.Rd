% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{ascClassifyConfidence}
\alias{ascClassifyConfidence}
\title{Classify each change High / Intermediate / Low confidence}
\usage{
ascClassifyConfidence(
  changes,
  evidence = NULL,
  thresholds = list(leiden = 0.9, resolution = 0.6, margin = 0, silhouette = 0, subsample
    = 0.8, support = 2)
)
}
\arguments{
\item{changes}{A data.frame from \code{ascPartitionChanges}.}

\item{evidence}{Optional data.frame joined to \code{changes} by row order with
any of the columns \code{leiden_stability}, \code{resolution_stability},
\code{margin}, \code{silhouette_delta}, \code{subsample_stability}. Missing
columns are treated as failing.}

\item{thresholds}{Named list of evidence thresholds (see defaults).}
}
\value{
\code{changes} with added \code{n_pass} and \code{confidence} columns.
}
\description{
Applies the asymmetric hysteresis rule: assigning new alleles and creating
brand-new ASCs are easy (auto High); splitting, merging, or reassigning
established ASCs requires reproducible evidence.
}
