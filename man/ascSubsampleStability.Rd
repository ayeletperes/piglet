% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{ascSubsampleStability}
\alias{ascSubsampleStability}
\title{Subsampling robustness of a proposed split (evidence E)}
\usage{
ascSubsampleStability(
  full_distance_matrix,
  reference_alleles,
  new_alleles,
  groups,
  resolution,
  frac = 0.8,
  n = 20,
  seed_base = 1000,
  ...
)
}
\arguments{
\item{full_distance_matrix}{A square distance matrix over reference + all new
alleles.}

\item{reference_alleles}{Character vector of the reference allele names.}

\item{new_alleles}{Character vector of the newly added allele names.}

\item{groups}{A list of character vectors of allele names (the proposed split).}

\item{resolution}{Leiden CPM resolution.}

\item{frac}{Fraction of new alleles kept per subsample (default 0.8).}

\item{n}{Number of subsamples (default 20).}

\item{seed_base}{Base seed; run \code{i} uses \code{seed_base + i}.}

\item{...}{Passed to \code{ascClusterOnce}.}
}
\value{
A list with \code{stability} (fraction of subsamples the groups
separate) and \code{n}.
}
\description{
Repeatedly subsamples the newly added alleles, re-clusters the reference plus
the subsample, and reports how often the proposed groups still separate.
}
