% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{ascClusterOnce}
\alias{ascClusterOnce}
\title{Single Leiden clustering with the fixed transform}
\usage{
ascClusterOnce(
  distance_matrix,
  resolution,
  seed = NULL,
  sim_method = "linear",
  floor = 0.001,
  min_similarity = 0
)
}
\arguments{
\item{distance_matrix}{A square distance matrix (or \code{dist}).}

\item{resolution}{Leiden CPM resolution.}

\item{seed}{Optional integer seed (\code{set.seed} before clustering).}

\item{sim_method, floor}{Passed to \code{.asc_fixed_similarity}.}

\item{min_similarity}{Passed to \code{.asc_build_graph}.}
}
\value{
A named integer membership vector (names = allele labels).
}
\description{
Runs one Leiden (CPM) community detection on the fixed, dataset-independent
similarity graph. Because \code{igraph}'s Leiden is randomised, \code{seed}
makes a run reproducible and lets callers sample the run-to-run distribution.
}
\examples{
data(HVGERM)
d <- igDistance(HVGERM[1:20], method = "decipher")
m <- ascClusterOnce(d, resolution = 0.1, seed = 1)

}
