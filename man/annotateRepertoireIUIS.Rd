% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_iuis_labels.R
\name{annotateRepertoireIUIS}
\alias{annotateRepertoireIUIS}
\title{Add IUIS gene-group columns to a genotype-corrected repertoire}
\usage{
annotateRepertoireIUIS(
  data,
  vocabulary,
  chain,
  v_gene = "v_gene",
  j_gene = "j_gene",
  d_gene = "d_gene",
  d_call = "d_call_new"
)
}
\arguments{
\item{data}{A \code{data.frame} of rearrangements carrying the ASC gene calls.}

\item{vocabulary}{An \code{ascIUISVocabulary} from \code{\link{ascIUISVocabulary}}.}

\item{chain}{The locus of \code{data}, one of \code{"IGH"}, \code{"IGK"}, \code{"IGL"}.}

\item{v_gene, j_gene, d_gene}{Column names holding the ASC gene calls. Defaults
\code{"v_gene"}, \code{"j_gene"}, \code{"d_gene"}.}

\item{d_call}{Column name holding the ASC \emph{allele} call for D, used for IGH only.
Default \code{"d_call_new"}.}
}
\value{
\code{data} as a \code{data.table} with the \code{*_gene_iuis} columns added.
}
\description{
Translates the ASC gene calls on a repertoire into the IUIS labels built by
\code{\link{ascIUISVocabulary}}, adding \code{v_gene_iuis}, \code{j_gene_iuis} and,
where the chain has one, \code{d_gene_iuis}.
}
\details{
For IGH the D label is per allele, so it is mapped from the allele-level call rather
than from the collapsed gene, and a call spanning more than one ASC group names no
single gene and stays \code{NA}. V and J map straight from their ASC gene.

Rows in and rows out are reported: a call that maps to nothing is left \code{NA} rather
than dropped, and the count is messaged so a labelling gap cannot pass unnoticed.
}
\seealso{
\code{\link{ascIUISVocabulary}}
}
