% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{runIncrementalASCExperiment}
\alias{runIncrementalASCExperiment}
\title{Run the incremental-ASC experiment end to end}
\usage{
runIncrementalASCExperiment(
  reference,
  new_seqs,
  baseline_assign = NULL,
  distance_method = "decipher",
  trim_3prime_side = 318,
  resolution = NULL,
  resolution_grid = seq(0.02, 0.3, by = 0.02),
  sim_method = "linear",
  floor = 0.001,
  n_leiden = 100,
  n_subsample = 20,
  quiet = TRUE
)
}
\arguments{
\item{reference}{Named character vector of reference germline sequences
(IMGT-gapped), or a \code{GermlineCluster} whose \code{alleleClusterTable}
provides the baseline assignment.}

\item{new_seqs}{Named character vector of new germline sequences to add.}

\item{baseline_assign}{Optional named vector allele -> original ASC. If
\code{NULL}, it is computed by clustering \code{reference}.}

\item{distance_method}{Distance metric passed to \code{igDistance} (default
\code{"decipher"}).}

\item{trim_3prime_side}{Fixed trim position for a stable distance frame
(default 318; \code{NULL} to disable).}

\item{resolution}{Leiden CPM resolution; if \code{NULL} it is chosen by a
silhouette sweep over \code{resolution_grid}.}

\item{resolution_grid}{Resolutions swept when \code{resolution} is \code{NULL}.}

\item{sim_method}{Fixed similarity transform, \code{"linear"} (\code{S = 1 - d})
or \code{"log"} (see \code{.asc_fixed_similarity}). \code{"log"} spreads
separation across a gentler resolution band and is preferable for larger,
more diverse references.}

\item{floor}{Constant floor for the \code{"log"} transform (default 1e-3).}

\item{n_leiden}{Seeded Leiden repetitions for the stability evidence (default
100).}

\item{n_subsample}{Subsamples for the robustness evidence (default 20).}

\item{quiet}{Suppress progress messages (default TRUE).}
}
\value{
An \code{IncrementalASCResult} S3 object (a list) with elements
\code{old_assign}, \code{new_assign}, \code{new_alleles}, \code{resolution},
\code{retention}, \code{changes} (with confidence), \code{reconciled},
\code{diagnostics} (a list of tables), and \code{distance_matrix}.
}
\description{
Starts from a reference and its ASC assignments, adds a batch of new sequences,
re-runs Leiden (CPM) with the fixed, dataset-independent transform on the
expanded set, and produces retention / change / evidence / confidence
diagnostics plus a reconciled ASC table.
}
\examples{
\donttest{
data(HVGERM)
ref <- HVGERM[1:60]
new <- HVGERM[61:80]
res <- runIncrementalASCExperiment(ref, new, n_leiden = 20, n_subsample = 5)
res$diagnostics$summary_counts
}

}
