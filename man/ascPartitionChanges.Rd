% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asc_incremental.R
\name{ascPartitionChanges}
\alias{ascPartitionChanges}
\title{Classify how ASCs change after adding new alleles}
\usage{
ascPartitionChanges(
  old_assign,
  new_assign,
  new_alleles,
  tau_keep = 0.9,
  p_new = 0.8,
  p_share = 0.2
)
}
\arguments{
\item{old_assign}{Named vector allele -> original ASC label (original alleles).}

\item{new_assign}{Named vector allele -> new cluster label (original + new).}

\item{new_alleles}{Character vector of the newly added allele names.}

\item{tau_keep}{Retention at/above which an ASC is considered "kept together"
(default 0.9).}

\item{p_new}{Fraction of a new cluster that must be newly added alleles for it
to count as a brand-new ASC (default 0.8).}

\item{p_share}{Fraction of an original ASC (or of a new cluster's old members)
that a group must hold to count toward a split / merge (default 0.2).}
}
\value{
A data.frame, one row per detected change, with columns
\code{change_type}, \code{old_asc}, \code{new_cluster}, \code{n_old},
\code{n_new_alleles}, \code{retention}, \code{support}, \code{members}.
\code{support} is the number of distinct new alleles associated with the
change (strong-evidence item F).
}
\description{
Labels each original ASC and each new cluster as one of: simple_expansion,
new_asc, candidate_split, candidate_merge, or reassignment.
}
