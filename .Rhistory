}
geno_V[i, count := allele_count]
}
i
allele = reference_alleles[i]
allele
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
if(is.null(likelihood_col)) {
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
allele_count <- multiple_assignments*single_assignments
}else{
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# get the count
return(1/length(multiple_assignments))
}))
allele_count <- multiple_assignments*single_assignments
}
allele_count
geno_V
geno_V <- data.table::data.table(allele = reference_alleles,
gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
count = numeric(),
frequency_threshold = alleleClusterTable$thresh,
repertoire_depth = numeric(),
z_score = numeric()
)
geno_V
alleles_count <- c()
for(i in 1:length(reference_alleles)) {
allele = reference_alleles[i]
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
## for every call the allele is in a single assignment, we will count it as 1.
## for multiple assignments, we will divide the count by the number of assignments or if likelihood col is provided by the normalized likelihood.
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
if(is.null(likelihood_col)) {
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
allele_count <- multiple_assignments*single_assignments
}else{
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# get the count
return(1/length(multiple_assignments))
}))
allele_count <- multiple_assignments*single_assignments
}
alleles_count <- c(alleles_count, allele_count)
}
geno_V <- data.table::data.table(allele = reference_alleles,
gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh,
repertoire_depth = sum(alleles_count),
)
geno_V <- data.table::data.table(allele = reference_alleles,
gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh,
repertoire_depth = sum(alleles_count)
)
length(alleles_count)
length(alleleClusterTable$thresh)
alleleClusterTable$thresh
alleleClusterTable <- read.delim("../aligners/genotype/piglet_asc_thresholds.tsv")
alleles_calls = alakazam::getAllele(data[[v_call]], first = FALSE,
strip_d = FALSE)
## check that the allele calls are in the supplied alleleClusterTable
unique_calls <- unique(unlist(strsplit(alleles_calls, ",")))
match <- unique_calls %in% alleleClusterTable$new_allele
if (!all(match)) {
for(allele in unique_calls[!match]) {
warning(paste0("The allele call ", allele, " is not in the alleleClusterTable. Using the default threshold: ", default_allele_threshold))
alleleClusterTable <- rbind(alleleClusterTable,
data.frame(new_allele = allele,
func_group = strsplit(allele,'[*]')[[1]][1],
imgt_allele = allele,
thresh = default_allele_threshold))
}
}
## check un mutated
if (find_unmutated) {
if (is.na(germline_db[1])) {
stop("germline_db needed if find_unmutated is TRUE")
}
alleles_calls <-
tigger::findUnmutatedCalls(alleles_calls, as.character(data[[seq]]),
germline_db)
if (length(alleles_calls) == 0) {
stop("No unmutated sequences found! Set 'find_unmutated' to 'FALSE'.")
}
}
## we will iterate trough the allele in the alleleClusterTable, and for each allele extract it's frequency.
## the base allele frequency will be 1 over the number of unique alleles in the alleleClusterTable.
alleleClusterTable <- alleleClusterTable[!duplicated(alleleClusterTable$new_allele)]
reference_alleles <- alleleClusterTable$new_allele
## we will iterate trough the allele in the alleleClusterTable, and for each allele extract it's frequency.
## the base allele frequency will be 1 over the number of unique alleles in the alleleClusterTable.
alleleClusterTable <- alleleClusterTable[!duplicated(alleleClusterTable$new_allele),]
alleleClusterTable
reference_alleles <- alleleClusterTable$new_allele
length(reference_alleles)
nrow(alleleClusterTable)
base_freq <- 1 / length(reference_alleles)
alleles_count <- c()
for(i in 1:length(reference_alleles)) {
allele = reference_alleles[i]
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
## for every call the allele is in a single assignment, we will count it as 1.
## for multiple assignments, we will divide the count by the number of assignments or if likelihood col is provided by the normalized likelihood.
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
if(is.null(likelihood_col)) {
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
allele_count <- multiple_assignments*single_assignments
}else{
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# get the count
return(1/length(multiple_assignments))
}))
allele_count <- multiple_assignments*single_assignments
}
alleles_count <- c(alleles_count, allele_count)
}
geno_V <- data.table::data.table(allele = reference_alleles,
gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh,
repertoire_depth = sum(alleles_count)
)
geno_V
geno_V <-
geno_V[, "z_score" := z_score(Ni=get("count"),N=get("repertoire_depth"),Pi=get("frequency_threshold"))]
z_score <- function(Ni, N, Pi) (Ni - Pi*N) / sqrt(Pi*N*(1-Pi))
geno_V <-
geno_V[, "z_score" := z_score(Ni=get("count"),N=get("repertoire_depth"),Pi=get("frequency_threshold"))]
alleleClusterTable <- read.delim("../aligners/genotype/piglet_asc_thresholds.tsv")
## we will iterate trough the allele in the alleleClusterTable, and for each allele extract it's frequency.
## the base allele frequency will be 1 over the number of unique alleles in the alleleClusterTable.
if (any(duplicated(alleleClusterTable$new_allele))) {
alleleClusterTable <- setDT(alleleClusterTable)
alleleClusterTable <-
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = mget(names(alleleClusterTable)[names(alleleClusterTable) !=
"imgt_allele"])]
}
nrow(alleleClusterTable)
any(duplicated(alleleClusterTable$new_allele))
alleleClusterTable <- setDT(alleleClusterTable)
alleleClusterTable <-
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = mget(names(alleleClusterTable)[names(alleleClusterTable) !=
"imgt_allele"])]
alleleClusterTable
View(alleleClusterTable)
duplicated(alleleClusterTable$new_allele)
which(duplicated(alleleClusterTable$new_allele))
alleleClusterTable[12]
alleleClusterTable[alleleClusterTable=="IGHVF1-G3*06",]
alleleClusterTable[alleleClusterTable$new_allele=="IGHVF1-G3*06",]
View(alleleClusterTable)
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = mget(names(alleleClusterTable)[names(alleleClusterTable) !=
"imgt_allele"])]
alleleClusterTable <-
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = get("new_allele")]
alleleClusterTable[alleleClusterTable$new_allele=="IGHVF1-G3*06",]
alleleClusterTable <- read.delim("../aligners/genotype/piglet_asc_thresholds.tsv")
alleleClusterTable <- setDT(alleleClusterTable)
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = get("new_allele")]
alleleClusterTable <-
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = get("new_allele")]
alleleClusterTable
alleleClusterTable <- read.delim("../aligners/genotype/piglet_asc_thresholds.tsv")
alleleClusterTable <- setDT(alleleClusterTable)
alleleClusterTable <-
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = "new_allele"]
alleleClusterTable
alleleClusterTable[alleleClusterTable$new_allele=="IGHVF1-G3*06",]
alleleClusterTable <- read.delim("../aligners/genotype/piglet_asc_thresholds.tsv")
alleleClusterTable <- setDT(alleleClusterTable)
alleleClusterTable <-
alleleClusterTable[, .("imgt_allele" = paste0(sort(unlist(unique(mget(
c("imgt_allele")
)))), collapse = "/")), by = c("new_allele","thresh")]
alleleClusterTable
reference_alleles <- alleleClusterTable$new_allele
base_freq <- 1 / length(reference_alleles)
alleles_count <- c()
for(i in 1:length(reference_alleles)) {
allele = reference_alleles[i]
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
## for every call the allele is in a single assignment, we will count it as 1.
## for multiple assignments, we will divide the count by the number of assignments or if likelihood col is provided by the normalized likelihood.
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
if(is.null(likelihood_col)) {
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
allele_count <- multiple_assignments*single_assignments
}else{
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# get the count
return(1/length(multiple_assignments))
}))
allele_count <- multiple_assignments*single_assignments
}
alleles_count <- c(alleles_count, allele_count)
}
geno_V <- data.table::data.table(allele = reference_alleles,
gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh,
repertoire_depth = sum(alleles_count)
)
z_score <- function(Ni, N, Pi) (Ni - Pi*N) / sqrt(Pi*N*(1-Pi))
geno_V <-
geno_V[, "z_score" := z_score(Ni=get("count"),N=get("repertoire_depth"),Pi=get("frequency_threshold"))]
geno_V
geno_V
geno_V <- data.table::data.table(gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
allele = reference_alleles,
imgt_allele = alleleClusterTable$imgt_allele,
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh,
repertoire_depth = sum(alleles_count)
)
geno_V <-
geno_V[, "z_score" := z_score(Ni=get("count"),N=get("repertoire_depth"),Pi=get("frequency_threshold"))]
geno_V
geno_V
geno_V <- data.table::data.table(gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
allele = reference_alleles,
imgt_allele = alleleClusterTable$imgt_allele,
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh,
)
geno_V <- data.table::data.table(gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
allele = reference_alleles,
imgt_allele = alleleClusterTable$imgt_allele,
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh
)
geno_V <-
geno_V[, "z_score" := z_score(Ni=get("count"),N=sum(alleles_count),Pi=get("frequency_threshold"))]
geno_V
geno_V
alleles_count <- c()
for(i in 1:length(reference_alleles)) {
allele = reference_alleles[i]
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
## for every call the allele is in a single assignment, we will count it as 1.
## for multiple assignments, we will divide the count by the number of assignments or if likelihood col is provided by the normalized likelihood.
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
if(is.null(likelihood_col)) {
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
allele_count <- multiple_assignments*single_assignments+single_assignments
}else{
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# get the count
return(1/length(multiple_assignments))
}))
allele_count <- multiple_assignments*single_assignments+single_assignments
}
alleles_count <- c(alleles_count, allele_count)
}
genotype <- data.table::data.table(gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
allele = reference_alleles,
imgt_allele = alleleClusterTable$imgt_allele,
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh
)
genotype <-
genotype[, "z_score" := z_score(Ni=get("count"),N=sum(alleles_count),Pi=get("frequency_threshold"))]
genotype
genotype
genotype <-
genotype[, "count" := round(get("count"), 5)]
genotype
genotype
View(genotype)
length(reference_alleles)
any(duplicated(alleleClusterTable$new_allele))
which(duplicated(alleleClusterTable$new_allele))
alleleClusterTable[47]
alleleClusterTable[alleleClusterTable$new_allele=="IGHVF3-G11*03",]
alleleClusterTable <- read.delim("../aligners/genotype/piglet_asc_thresholds.tsv")
alleleClusterTable <- setDT(alleleClusterTable)
alleleClusterTable <-
alleleClusterTable[, .(
"imgt_allele" = paste0(sort(unlist(unique(mget(c("imgt_allele"))))), collapse = "/"),
"thresh" = min(get("thresh"))), by = c("new_allele")]
alleleClusterTable[alleleClusterTable$new_allele=="IGHVF3-G11*03",]
reference_alleles <- alleleClusterTable$new_allele
base_freq <- 1 / length(reference_alleles)
alleles_count <- c()
for(i in 1:length(reference_alleles)) {
allele = reference_alleles[i]
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
## for every call the allele is in a single assignment, we will count it as 1.
## for multiple assignments, we will divide the count by the number of assignments or if likelihood col is provided by the normalized likelihood.
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
if(is.null(likelihood_col)) {
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
allele_count <- multiple_assignments*single_assignments+single_assignments
}else{
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# get the count
return(1/length(multiple_assignments))
}))
allele_count <- multiple_assignments*single_assignments+single_assignments
}
alleles_count <- c(alleles_count, allele_count)
}
genotype <- data.table::data.table(gene = alakazam::getGene(reference_alleles, first = F, collapse = T, strip_d = F),
allele = reference_alleles,
imgt_allele = alleleClusterTable$imgt_allele,
count = alleles_count,
frequency_threshold = alleleClusterTable$thresh
)
z_score <- function(Ni, N, Pi) (Ni - Pi*N) / sqrt(Pi*N*(1-Pi))
genotype <-
genotype[, "z_score" := z_score(Ni=get("count"),N=sum(alleles_count),Pi=get("frequency_threshold"))]
genotype <-
genotype[, "count" := round(get("count"), 5)]
head(data[grep("IGHVF1-G2[*]01",data$v_call_cap),])
data2 <- read.csv("../aligners/genotype/match_data/1.csv")
head(data2[grep("IGHVF1-G2[*]01",data2$v_call),])
allele = "IGHVF1-G2*01"
## grep the indices of the allele calls
allele_indices <- grep(gsub("\\*", "\\\\*", allele), alleles_calls)
allele_calls <- alleles_calls[allele_indices]
# count single assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments <- ifelse(single_assignments==0, base_freq, single_assignments)
single_assignments
# count multiple assignments
multiple_assignments_indices <- grep(",", allele_calls)
likelihoods <- data[[likelihood_col]][allele_indices]
likelihoods <- likelihoods[multiple_assignments_indices]
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
multiple_assignments
single_assignments
multiple_assignments*single_assignments
1/200
idx = multiple_assignments_indices[1]
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
allele_loc <- which(multiple_assignments == allele)
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
likelihood
single_assignments
multiple_assignments_wheight <- (single_assignments+1) / length(reference_alleles)
multiple_assignments*multiple_assignments_wheight+single_assignments
multiple_assignments_wheight
single_assignments
single_assignments <- sum(!grepl(",", allele_calls))
single_assignments
multiple_assignments
multiple_assignments <- sum(sapply(multiple_assignments_indices, function(idx){
multiple_assignments <- unlist(strsplit(allele_calls[idx], ","))
# get the allele loc
allele_loc <- which(multiple_assignments == allele)
# normalize the likelihood
likelihood <- as.numeric(unlist(strsplit(likelihoods[idx], ",")))
likelihood <- likelihood/sum(likelihood)
# get the count
return(likelihood[allele_loc])
}))
multiple_assignments*single_assignments+single_assignments
multiple_assignments*multiple_assignments_wheight+single_assignments
nrow(data2[grep("IGHVF1-G2[*]01",data2$v_call),])
nrow(data2[grep("IGHVF1-G2[*]02",data2$v_call),])
genotype
genotype
alakazam::progressBar
library(tigger)
